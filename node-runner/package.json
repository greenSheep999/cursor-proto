{
  "name": "cursor-proxy-node-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node.js child process that runs @cursor/sdk on behalf of cursor-proxy. Speaks line-delimited JSON-RPC over stdio; the Go supervisor spawns exactly one of these per proxy process. See docs/sdk-integration.md for the protocol contract.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node --test dist/*.test.js",
    "test:dev": "tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@cursor/sdk": "1.0.23"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
